name: Trigger auto deployment for hdbcontapp

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/hdbcontapp-AutoDeployTrigger-edb838c3-e32d-4577-9cdd-04f111ec31c7.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.HDBCONTAPP_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.HDBCONTAPP_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.HDBCONTAPP_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: 
          registryUsername: ${{ secrets.HDBCONTAPP_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.HDBCONTAPP_REGISTRY_PASSWORD }}
          containerAppName: hdbcontapp
          resourceGroup: DefaultResourceGroup-SEA
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


